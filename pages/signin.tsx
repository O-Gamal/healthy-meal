import type { NextPage } from 'next';
import Layout from '../components/Layout';

const SigninPage: NextPage = () => {
  return (
    <Layout noSaerch title="Sign In">
      SigninPage
    </Layout>
  );
};
export default SigninPage;
